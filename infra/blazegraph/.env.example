# ═══════════════════════════════════════════════
# Neptune Emulator (Blazegraph) - Base Configuration
# ═══════════════════════════════════════════════

# SPARQL endpoint port (Blazegraph standard is 9999)
NEPTUNE_PORT=9999

# Container memory limits
# Light usage: 1g / 512m
# Dev usage:   2g / 1g (default)
# Heavy usage: 4g / 2g
NEPTUNE_MEMORY_LIMIT=2g
NEPTUNE_MEMORY_RESERVATION=1g

# Health check configuration
NEPTUNE_HEALTH_INTERVAL=30s
NEPTUNE_HEALTH_TIMEOUT=10s
NEPTUNE_HEALTH_RETRIES=3
NEPTUNE_HEALTH_START_PERIOD=40s

# Logging configuration (prevent disk fill-up)
NEPTUNE_LOG_MAX_SIZE=10m
NEPTUNE_LOG_MAX_FILE=3

# Timezone for logs and timestamps
TZ=UTC

# Optional: JVM tuning overrides (uncomment to customize)
# JAVA_OPTS=-XX:MaxRAMPercentage=60.0 -XX:InitialRAMPercentage=60.0
